---
description: Portfolio dunes architecture, hard constraints, and domain layout
alwaysApply: true
---

# Portfolio dunes (SNOWFLOW engine)

Walkable sand portfolio on WebGPU (Babylon.js + hand-written WGSL). **No WebGL fallback.** Sand-only in product mode: no surf, no castable spells. Authored GLB props + procedural terrain/deform/sky + bow combat.

## Layout

```
src/main.js          entry + frame orchestration (order is load-bearing)
src/core/            settings, input, camera, loading, gpuUtil, envProfile
src/terrain/         heightfield, clipmap, deformation state buffer
src/render/          sky/IBL, shadow cascades, depth prepass
src/character/       controller, PlayerAvatar (GLB), snowContact
src/props/           Giant, Antinous (sun-summon patrol enemy)
src/combat/          bow, arrow pool, sfx helpers
src/portfolio/       pedestals, cards, projects.json
src/vfx/             spray particles
src/spells/          SpellLights pool only (pedestal key lights)
src/post/            TAA, bloom, shafts, DoF, SSR, tonemap
src/shaders/         WGSL; lib/ = shared #includes
```

Legacy procedural figure/cloth modules may still exist under `character/` but are **unwired** — live path is `controller` + `avatar` + `snowContact(..., null, spray)`.

## Hard constraints

- **No allocation in the render loop.** Pre-size buffers/typed arrays; module-scope scratch (`_vel`, `_aim`). Never `new` per frame.
- **Warm up behind the loading screen.** Materials / pipelines / SFX `warmUp()` / `whenReady()` / `preloadSfx` before first interactive frame. Boot UI is video + bar + phase text only (see `index.html` / `loading.js`).
- **Keep assets lean.** Crush GLBs; no unused portfolio thumbs; sand detail only — see `assets-policy`.
- **Keep systems modular.** New features go in their domain folder. Wire in `main.js` only for construction + per-frame order.
- **Shared sand state.** Feet + props write deform via `DeformationField.brush()`. Lit props/terrain read `SpellLights` / `#include<snowSpellLights>`.
- **Rendering groups:** 0 = sky, 1 = opaque, 2 = alpha (spray). Depth auto-clear between groups is off on purpose.
- Prefer lean changes. No fallbacks, catch-alls, or speculative branches.
- **No physics engine.** Kinematic controller + circle push-outs + **melee contact** (hand/body proximity in a timed window).

## Run

```bash
npm install && npm run dev   # http://localhost:5173
npm run check
```
